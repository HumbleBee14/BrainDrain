import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default OpenNext → Cloudflare Workers config. Add caching/queue overrides here
// later if needed (R2 incremental cache, etc.).
export default defineCloudflareConfig();
